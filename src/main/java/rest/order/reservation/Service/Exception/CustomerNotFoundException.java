package rest.order.reservation.Service.Exception;

// error 가 생긴 경우 이렇게 넣으라고 하는데 당장은 넣기 좀 그래서 패스 
// 안쓸듯 ㅎ 
public class CustomerNotFoundException {

    /*
     * 
     * .map(customerDTOMapper)
     *   .orElseThrow(() -> new CustomerNotFoundException("Customer not found with id: " + customerInfo.id())) 
     *  return customerDTO;
     * 
     */
}
